# Postgres connection (matches docker-compose.yml defaults for local dev)
DATABASE_URL="postgresql://forever:forever@localhost:5432/forever?schema=public"

# Auth.js secret — generate with: npx auth secret  (or: openssl rand -base64 32)
AUTH_SECRET="replace-me-with-a-random-32-byte-base64-string"
